# ── 依赖 ──────────────────────────────────────────────
# 本项目零依赖（package.json 无 dependencies），留作以后保险
node_modules/

# ── 打包产物 ──────────────────────────────────────────
# dist/haixiao-minitool.zip 由 `npm run build` 随时重建，不入库。
# 想让仓库自带成品 zip 的话，把下面这行删掉即可（44KB，不大）。
dist/

# ── macOS 压缩包解出来的垃圾 ──────────────────────────
# _skill/ 是从 zip 解压来的，带了 __MACOSX 资源叉，纯噪音
__MACOSX/
._*

# ── 平台 skill 包 ─────────────────────────────────────
# .skill/minitool-zip-builder/ 已解包入库（提审依据，见 docs/发布校验记录.md）。
# 根目录的 .skill 压缩包只是它的原始形态，内容重复，不入库。
minitool-zip-builder-*.skill

# ── 编辑器 / 操作系统 ─────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ── 日志 ──────────────────────────────────────────────
*.log
npm-debug.log*

# 本地浏览器验证的 Python 缓存
__pycache__/
*.pyc
