# ── 依赖 ──────────────────────────────────────────────
# 本项目零依赖（package.json 无 dependencies），留作以后保险
node_modules/

# ── 打包产物 ──────────────────────────────────────────
# dist/haixiao-minitool.zip 由 `npm run build` 随时重建，不入库。
# 想让仓库自带成品 zip 的话，把下面这行删掉即可（44KB，不大）。
dist/

# ── macOS 压缩包解出来的垃圾 ──────────────────────────
# _skill/ 是从 zip 解压来的，带了 __MACOSX 资源叉，纯噪音
__MACOSX/
._*

# ── 编辑器 / 操作系统 ─────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ── 日志 ──────────────────────────────────────────────
*.log
npm-debug.log*
